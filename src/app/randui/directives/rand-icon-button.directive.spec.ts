import { RandIconButtonDirective } from './rand-icon-button.directive';

describe('RandIconButtonDirective', () => {
  it('should create an instance', () => {
    // const directive = new RandIconButtonDirective();
    // expect(directive).toBeTruthy();
  });
});
